@model HomeViewModel

<head>
    <link href="~/Content/Site.css" rel="stylesheet" />
</head>

<h2>Prefered cars of the month</h2>
<h4>Clients preferences - just for you!</h4>

<div class="row">
    @foreach (var car in Model.PreferredCars)
    {
        @await Html.PartialAsync("CarSummary", car)
    }
</div>